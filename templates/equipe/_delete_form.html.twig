<form method="post" action="{{ path('app_equipe_delete', {'id_equipe': equipe.id_equipe}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipe.id_equipe) }}">
    <button class="btn">Delete</button>
</form>
